const getId = (id) => "#" + id;
const getClass = (className) => "." + className;
const getProperty = (property, value) => "[" + property + "='" + value + "']";

export { getId, getClass, getProperty };