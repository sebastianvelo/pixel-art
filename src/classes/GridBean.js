class GridBean {
    constructor(rows, columns, size, border) {
        this.rows = rows;
        this.columns = columns;
        this.size = size;
        this.border = border;
    }
}
export default GridBean;